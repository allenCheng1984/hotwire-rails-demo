  <div class="card card-body">
    <div><%= tweet.body %></div>

    <div class="mt-2">
      <%= button_to "Likes (#{tweet.likes_count})", '#', method: :post %>
      <%= button_to "Retweets (#{tweet.retweets_count})", '#', method: :post %>

      <%= link_to "Edit", edit_tweet_path(tweet) %>
    </div>
  </div>
